// let é o iniciador, sem ela não podemos iniciar a
// a variavel

// seguido declaramos o nome da variavel
// Depois declaramos o valor que a variavel vai receber, variaveis podem ser criadas sem valor.

let nome = "Super man"
let idade = 100
let poder = "Super força"

nome = "Batman"
idade = 32
poder = "Rico"

console.log(
  `Nome: ${nome}
   Idade: ${idade}
   Poder: ${poder}`
)

//No javascript, existem palavras revervadas que não podem ser usadas como variaves, pois elas já são próprias do javascript.

